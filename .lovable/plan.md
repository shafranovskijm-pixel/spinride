
# План: Показ имени пользователя в хедере

## Что будет сделано

После авторизации вместо простой иконки пользователя в хедере появится:
- Аватар с инициалами или фото пользователя
- Имя пользователя (если задано) или email
- Выпадающее меню с действиями

## Визуальный результат

**Для гостя:** Иконка пользователя -> переход на /auth/login

**Для авторизованного пользователя:**
```text
+------------------+
|  [Аватар] Имя ▼ |
+------------------+
      |
      v
+------------------+
| 👤 Мой профиль  |
| 📦 Мои заказы   |
| ⚙️  Админка *   |
|------------------|
| 🚪 Выйти        |
+------------------+
* только для админов
```

## Технические изменения

### Файл: `src/components/shop/Header.tsx`

1. Добавить импорт `useAuth` и компонентов:
   - `Avatar`, `AvatarImage`, `AvatarFallback`
   - `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuSeparator`
   - `LogOut`, `Settings`, `Package` иконки из lucide-react

2. Получить данные из `useAuth()`:
   - `user` - текущий пользователь
   - `profile` - профиль с display_name и avatar_url
   - `isAdmin` - флаг администратора
   - `signOut` - функция выхода

3. Заменить блок "User/Admin" (строки 100-105) на условный рендеринг:

   **Если пользователь не авторизован:**
   - Ссылка на `/auth/login` с иконкой User

   **Если пользователь авторизован:**
   - DropdownMenu с триггером-аватаром
   - Аватар показывает:
     - Фото из `profile.avatar_url` (если есть)
     - Инициалы из имени или первую букву email (fallback)
   - Рядом с аватаром - имя пользователя (только на десктопе)
   - В меню:
     - "Мой профиль" (ссылка на будущую страницу профиля)
     - "Мои заказы" (ссылка на будущую страницу заказов)
     - "Админка" (только для isAdmin)
     - Разделитель
     - "Выйти" (вызывает signOut)

4. Добавить хелпер-функцию `getInitials()`:
   ```typescript
   const getInitials = (name: string | null, email: string) => {
     if (name) {
       return name.split(' ').map(n => n[0]).join('').toUpperCase().slice(0, 2);
     }
     return email[0].toUpperCase();
   };
   ```

5. Обновить мобильное меню (Sheet):
   - Добавить секцию пользователя вверху
   - Показать аватар + имя
   - Добавить кнопку выхода

## Зависимости

Все необходимые компоненты уже есть в проекте:
- `@radix-ui/react-avatar` - установлен
- `@radix-ui/react-dropdown-menu` - установлен
- `useAuth` хук - реализован
- Lucide иконки - установлены

## Результат

- Авторизованный пользователь видит свой аватар и имя в хедере
- Быстрый доступ к профилю, заказам и выходу через dropdown
- Админы видят дополнительную ссылку на админку
- Мобильная версия также показывает информацию о пользователе
